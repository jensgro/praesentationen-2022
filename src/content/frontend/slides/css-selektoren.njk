<section class="shouting">
    <h2>Selektoren</h2>
</section>

<section>
    <pre><code class="css large-text">a { color: red;}

p a { color: blue;}

p > a { color: yellow;}

p > span > a { color: brown;}

ul + ul { margin-top: 20px;}

* + * { margin-bottom: 10px;}

p ~ h2 { border-left: 5px solid red;}</code></pre>
</section>

<section>
    <pre><code class="css large-text">button.cta-1 { }

header#main-header { }

p:first-child { }

p:last-child { }

p:only-of-type { }
</code></pre>    
</section>

<section>
    <pre><code class="css large-text">p:nth-child(3) { }

p:nth-child(2n+4) { }

p:nth-of-type(2) { }

p:nth-of-type(odd) { }

p:nth-last-child(3) { }</code></pre>
    
</section>

<section>
    <pre><code class="css large-text">button:not(.cta-2) { }

p:empty { }

:valid { }

:invalid { }

:required { }

:root {}   /* a.k.a. html {} */</code></pre>
</section>

<section>
    <pre><code class="css">input[type="checkbox"] { }

input[type="email"] { }

a[href] { }

a[name] { }

a[href^="http://"] {background-image: url("images/outside-link.gif");}

a[href$=".pdf"] { background-image: url("images/pdf-icon.gif"); }

a[href*="images/"] { background-image: url("images/pdf-icon.gif"); }</code></pre>

</section>

<section>
    <h2>Alte und neue Schreibweise</h2>    
    <h3>Alte Schreibweise</h3>
    <pre><code class="css large-text">button:before { content:""; }
.clearfix:after { content:""; }</code></pre>

    <h3>Neue Schreibweise</h3>
    <pre><code class="css large-text">button::before { content:""; }
.clearfix::after { content:""; }</code></pre>
</section>

<section>
    <img src="pictures/frontend/attributselektoren.png" class="stretch" alt="Ein Codepenbeispiel mit Attributselektoren">
    <div class="linksToOutside"><a href="https://codepen.io/jensgro/pen/atujC?editors=0100" target="_blank">Codepen</a></div>
</section>
